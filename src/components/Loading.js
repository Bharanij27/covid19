import React from 'react';
import ReactLoading from "react-loading";

const Loading = () => {
    return(
            <ReactLoading  className="loading" type='bubbles' color='#fff' height={'8%'} width={'8%'}/>
    )
}

export default Loading;